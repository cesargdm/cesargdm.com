import * as React from 'react'

import DefaultLayout from '../../layouts/Default'

function Uses() {
  return (
    <DefaultLayout>
      <h1>Blog</h1>
    </DefaultLayout>
  )
}

export default Uses
