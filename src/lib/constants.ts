export const BASE_URL = 'https://cesargdm.com'

/** Shared `Person` identity for structured data (site-wide and per-post JSON-LD). */
export const AUTHOR = {
	name: 'César Guadarrama Cantú',
	url: BASE_URL,
}
